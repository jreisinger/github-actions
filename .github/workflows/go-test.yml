name: go workflows test

on: [push]

jobs:
  go:
    uses: jreisinger/github-actions/.github/workflows/go.yml@main
  go-release:
    needs:
      - go
    uses: jreisinger/github-actions/.github/workflows/go-releaser.yml@main