name: test, scan and release

on:
  pull_request:
    branches:
      - main
  push:
    tags:
      - "v*"

jobs:
  go-test:
    uses: jreisinger/github-actions/.github/workflows/go-test.yml@main
  trivy-scan:
    uses: jreisinger/github-actions/.github/workflows/trivy-scan.yml@main
  go-release:
    needs:
      - go-test
      - trivy-scan
    uses: jreisinger/github-actions/.github/workflows/go-release.yml@main
